diff -Naur a/src/useradd.c b/src/useradd.c
--- a/src/useradd.c	2026-02-05 05:13:59.281134231 +0100
+++ b/src/useradd.c	2026-02-05 05:20:29.570664134 +0100
@@ -2329,7 +2329,6 @@
 	gid_t         gid;
 	mode_t        mode;
 	const char    *spool;
-	struct group  *gr;
 	bool          process_selinux;
 
 	process_selinux = !flags->chroot && !flags->prefix;
@@ -2370,16 +2369,8 @@
 		return;
 	}
 
-	gr = prefix_getgrnam("mail"); /* local, no need for xgetgrnam */
-	if (NULL == gr) {
-		fputs(_("Group 'mail' not found. Creating the user mailbox file with 0600 mode.\n"),
-		       stderr);
-		gid = user_gid;
-		mode = 0600;
-	} else {
-		gid = gr->gr_gid;
-		mode = 0660;
-	}
+	gid = user_gid;
+	mode = 0600;
 
 	if (fchown(fd, user_id, gid) != 0
 	 || fchmod(fd, mode) != 0)
