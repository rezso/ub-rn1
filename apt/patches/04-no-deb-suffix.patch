diff -Naur a/apt-pkg/deb/dpkgpm.cc b/apt-pkg/deb/dpkgpm.cc
--- a/apt-pkg/deb/dpkgpm.cc	2025-10-04 14:30:16.000000000 +0200
+++ b/apt-pkg/deb/dpkgpm.cc	2025-10-19 22:28:43.467335217 +0200
@@ -1877,8 +1877,6 @@
 	       if (I->File[0] != '/')
 		  return _error->Error("Internal Error, Pathname to install is not absolute '%s'",I->File.c_str());
 	       std::string file{flNotDir(I->File)};
-	       if (flExtension(file) != "deb")
-		  file.append(".deb");
 	       std::string linkpath;
 	       if (dpkg_recursive_install_numbered)
 		  strprintf(linkpath, "%s/%.*lu-%s", tmpdir_for_dpkg_recursive.get(), p, n, file.c_str());
