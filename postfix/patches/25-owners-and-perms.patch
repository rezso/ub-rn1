diff -Naur a/postfix-install b/postfix-install
--- a/postfix-install	2024-02-19 00:19:11.000000000 +0100
+++ b/postfix-install	2025-12-22 00:12:55.500990470 +0100
@@ -736,22 +736,26 @@
     # If installing from source code, apply special permissions or ownership.
     # If building a package, don't apply special permissions or ownership.
 
-    case $install_root in
-    "") case $owner in
+	case $owner in
 	[$]*) eval owner=$owner;;
 	root) owner=;;
 	esac
 	case $group in
 	[$]*) eval group=$group;;
 	   -) group=;;
-	esac;;
-     *) case $mode in
-	[1-7]755) mode=755;;
 	esac
-	owner=
-	group=;;
-    esac
 
+	# Cheat... these are not yet present in /etc/{passwd,group} while
+	# uhubuild is running
+	if [ _$owner = _postfix ]; then
+	    owner=25
+	fi
+	if [ _$group = _postfix ]; then
+	    group=25
+	fi
+	if [ _$group = _postdrop ]; then
+	    group=26
+	fi
 
     case $type in
 
